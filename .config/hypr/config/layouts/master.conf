# ╔═══════════════════════════════════════════════════════════════════╗
# ║                          Master Layout                           ║
# ╚═══════════════════════════════════════════════════════════════════╝
# Configuration for the master layout

master {
    # New windows become master
    new_status = master
    
    # New windows appear on top
    new_on_top = true
    
    # Allow small split ratio
    allow_small_split = false
    
    # Always center master window
   #  always_center_master = false
}
